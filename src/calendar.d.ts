// Short aliases for the Apps Script Advanced Calendar Service (Calendar v3) schema
// types. The `Calendar` global and the underlying schema types are provided by
// @types/google-apps-script; we only alias them here for readability.

type CalEvent = GoogleAppsScript.Calendar.Schema.Event;
type CalEventsList = GoogleAppsScript.Calendar.Schema.Events;
type CalReminderOverride = GoogleAppsScript.Calendar.Schema.EventReminder;
